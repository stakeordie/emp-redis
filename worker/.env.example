# Example environment configuration for EmProps Redis Worker
# Copy this file to .env and modify as needed

#==============================================================================
# CORE WORKER CONFIGURATION
#==============================================================================

# Redis Hub connection
# You can either specify host and port separately, or use REDIS_WS_URL for a direct WebSocket URL
#REDIS_API_HOST=redisserver-production.up.railway.app
#REDIS_API_PORT=443
REDIS_API_HOST=hub
REDIS_API_PORT=8001
# Alternative: Direct WebSocket URL (if set, REDIS_API_HOST and REDIS_API_PORT are ignored)
# REDIS_WS_URL=wss://redisserver-production.up.railway.app/ws/worker/worker-id

# SSL configuration
USE_SSL=false

# Worker identification
# Must be unique across all workers connected to the same Redis Hub
WORKER_ID=modular-worker-1

# Authentication
# IMPORTANT: This must match the token set on the Redis Hub
#WEBSOCKET_AUTH_TOKEN=3u8sdj5389fj3kljsf90u

# Connector configuration
# Comma-separated list of connectors to load
# Each connector must have a corresponding [connector_name]_connector.py file in the connectors directory
CONNECTORS=simulation,comfyui

# Worker settings
# Interval in seconds between heartbeat messages
HEARTBEAT_INTERVAL=20

# Logging
# Valid values: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

#==============================================================================
# CONNECTOR-SPECIFIC CONFIGURATION
#==============================================================================

#------------------------------------------------------------------------------
# Simulation Connector
#------------------------------------------------------------------------------
# Job type for the simulation connector (default: "simulation")
SIMULATION_JOB_TYPE=simulation

# Processing time in seconds for simulated jobs (default: 10)
SIMULATION_PROCESSING_TIME=10

# Number of steps for simulated jobs (default: 5)
SIMULATION_STEPS=5

#------------------------------------------------------------------------------
# ComfyUI Connector
#------------------------------------------------------------------------------
# ComfyUI server URL
COMFYUI_SERVER_URL=http://localhost:8188

# ComfyUI job type (default: "comfyui")
COMFYUI_JOB_TYPE=comfyui

# ComfyUI polling interval in seconds (default: 1)
COMFYUI_POLLING_INTERVAL=1

# Add additional connector-specific environment variables as needed
